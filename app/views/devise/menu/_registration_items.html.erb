<% if user_signed_in? %>
  Welcome <%= link_to current_user.username, show_user_path(current_user.username) %> (<%= link_to 'edit', edit_user_registration_path %>)
<% else %>
  <%= link_to 'Sign Up', new_user_registration_path, :class => 'capitalize_first' %>
<% end %>